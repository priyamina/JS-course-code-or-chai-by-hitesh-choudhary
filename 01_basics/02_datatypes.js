"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => stand alone value
// undefined => 
// symbol => unique
// object

console.log(typeof undefined); // undefined

console.log(typeof null); // object
/*differance between null and undefined: null:stand alone value hai ,representation of empty value.
example:if i request a server to give temp,but the server is unable to provide the temp. then it will return null 
&
undefined:simply value is not assigned only variable is declared */
